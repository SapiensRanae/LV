import React from 'react';
import './Slots.css';

const Slots: React.FC = () => {
    return (
        <div>
            <div className="slots">

            </div>
        </div>
    );
};

export default Slots;
